{
  "name": "playwright-demo-framework",
  "version": "1.0.0",
  "type": "commonjs",
  "scripts": {
    "pretest": "mkdir -p reports",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report",
    "test:with-report": "playwright test",
    "test:headed:with-report": "playwright test --headed",
    "install:browsers": "npx playwright install --with-deps"
  },
  "devDependencies": {
    "@faker-js/faker": "^9.8.0",
    "@playwright/test": "^1.53.1",
    "@types/node": "^24.0.3",
    "@zealteam/testrail-reporter": "^1.0.17",
    "chalk": "^5.3.0",
    "dotenv": "^16.5.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "axios": "^1.10.0",
    "form-data": "^4.0.3"
  }
}